# UI runs on :5173 — API runs on :3000/api.
# Default: Vite proxies `/api` on :5173 → :3000 (keeps login cookies working).
# Only uncomment if you need direct API calls (requires CORS; cookies may not work):
# VITE_API_BASE=http://localhost:3000/api
